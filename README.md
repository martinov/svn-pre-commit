SVN pre commit hook scripts
===========================

The main file is pre-commit - copy that in your repository hooks directory, e.g. /var/svn/project/hooks.

Edit the paths in this file to match the location where you've put them.
